<div id="game_status"
  class="absolute inset-0 flex flex-col items-center justify-center
         backdrop-blur-sm animate-fade-in"
  style="background: <%= game.won? ? 'rgba(0,20,0,0.85)' : 'rgba(20,0,0,0.85)' %>">

  <div class="text-center px-8 py-6 border
              <%= game.won? ? 'border-green-400' : 'border-red-500' %>"
       style="font-family: 'Courier New', monospace;
              box-shadow: 0 0 40px <%= game.won? ? '#22c55e' : '#ef4444' %>44;">

    <div class="text-5xl mb-4">
      <%= game.won? ? '⬛' : '💥' %>
    </div>

    <p class="text-xs tracking-[0.4em] uppercase mb-1
              <%= game.won? ? 'text-green-400' : 'text-red-400' %>">
      <%= game.won? ? 'champ dégagé' : 'contact mine' %>
    </p>

    <h2 class="text-3xl font-bold mb-6 tracking-tight
               <%= game.won? ? 'text-green-300' : 'text-red-300' %>">
      <%= game.won? ? 'VICTOIRE' : 'GAME OVER' %>
    </h2>

    <div class="text-xs text-gray-400 mb-6 space-y-1">
      <p><%= @game.revealed_cells.size %> cellules révélées</p>
      <p><%= @game.width %>×<%= @game.height %> · <%= (@game.mine_density * 100).to_i %>% mines</p>
    </div>

    <%= link_to "NOUVELLE PARTIE", new_game_path,
      class: "inline-block px-6 py-2 text-xs tracking-[0.3em] uppercase
              border transition-all duration-200
              #{game.won? ? 'border-green-400 text-green-400 hover:bg-green-400 hover:text-black' : 'border-red-500 text-red-400 hover:bg-red-500 hover:text-black'}" %>
  </div>
</div>
